	<div class="panel">
		<div class="panel-body">
			<%= image_tag message.user.avatar, class: "img-circle avatar-small" %>

			<strong><%= message.user.fullname %></strong>

			<span class="pull-right"><%= message.message_time %></span>
			<br>
			<div class="row-space-2">
				<%= message.body %>
			</div>
		</div>
	</div>
